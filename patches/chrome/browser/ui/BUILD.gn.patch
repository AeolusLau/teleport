diff --git a/chrome/browser/ui/BUILD.gn b/chrome/browser/ui/BUILD.gn
index 017a8c3ff755f..882519067ca02 100644
--- a/chrome/browser/ui/BUILD.gn
+++ b/chrome/browser/ui/BUILD.gn
@@ -393,6 +393,12 @@ static_library("ui") {
     "webui/fileicon_source.h",
     "webui/metrics_handler.cc",
     "webui/metrics_handler.h",
+
+    # teleport overlay: voluntary in-place enrollment entry point. Includes
+    # chrome/browser/ui headers, so it is compiled here rather than in the
+    # //teleport source_set (which would create a GN dependency cycle).
+    "//teleport/browser/enterprise/teleport_voluntary_signin.cc",
+    "//teleport/browser/enterprise/teleport_voluntary_signin.h",
   ]
 
   defines = []
@@ -437,6 +443,8 @@ static_library("ui") {
   ]
 
   deps = [
+    "//teleport",
+    "//teleport:teleport_deployment_config",
     ":browser_element_identifiers",
     ":browser_otr_state",
     ":browser_ui_prefs",
