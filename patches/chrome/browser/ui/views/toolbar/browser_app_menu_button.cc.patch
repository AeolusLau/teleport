diff --git a/chrome/browser/ui/views/toolbar/browser_app_menu_button.cc b/chrome/browser/ui/views/toolbar/browser_app_menu_button.cc
index 3f8871e197aa2..526dd12b53e0a 100644
--- a/chrome/browser/ui/views/toolbar/browser_app_menu_button.cc
+++ b/chrome/browser/ui/views/toolbar/browser_app_menu_button.cc
@@ -285,8 +285,12 @@ void BrowserAppMenuButton::UpdateTextAndHighlightColor() {
   } else if (type_and_severity_.type ==
              AppMenuIconController::IconType::kUpgradeNotification) {
     tooltip_message_id = IDS_APPMENU_TOOLTIP_UPDATE_AVAILABLE;
-#if BUILDFLAG(GOOGLE_CHROME_BRANDING) && \
-    (BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX))
+// teleport: upstream gates the randomized update-text variants behind
+// GOOGLE_CHROME_BRANDING. We ship our own Sparkle updater and want the same
+// rotating chip text, so enable it for desktop unbranded builds as well. The
+// ALT strings are platform-gated (not _google_chrome-gated) and are already in
+// the resource bundle, rebranded to Teleport / 闪现.
+#if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX)
     int message_id = IDS_APP_MENU_BUTTON_UPDATE;
     // Select an update text option randomly. Show this text in all browser
     // windows.
